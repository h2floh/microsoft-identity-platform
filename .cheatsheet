## URL Encode with jq

echo '' | jq -sRr @uri

## Well Known URL suffix

.well-known/openid-configuration

## Wrap x5c Signature in jwt.io verification box 

```bash
-----BEGIN PUBLIC KEY-----
-----END PUBLIC KEY-----
```

## Run dotnet with https (from 7.0 upwards)

cerate dev certs
```bash
dotnet dev-certs https --trust
```

launch in https mode with automatic rebuild
```bash
dotnet watch run --launch-profile https
```
